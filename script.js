const y = prompt("Enter the number of the table...")
for (let a = y; a <= 20; a++){
    for (let m = 1; m<=10; m++){
        result =m *a
    document.write(`${a} x ${m} =${result} <br>`)

    }
    document.write("<br>")
}

